{
  "worktree": {
    "setup": ["cp \"$PASEO_SOURCE_CHECKOUT_PATH/.env\" .env", "bun run scripts/init_worktree.ts"],
    "teardown": "bun run dev:stop"
  },
  "scripts": {
    "dev": {
      "type": "task",
      "command": "bun run dev --no-open"
    },
    "dev:full": {
      "type": "task",
      "command": "bun run dev:full --no-open"
    },
    "dev:pglite": {
      "type": "task",
      "command": "bun run dev:pglite --no-open"
    },
    "prep-dev": {
      "type": "task",
      "command": "PLEXUS_URL=http://localhost bun run prep-dev"
    },
    "prep-dev:save": {
      "type": "task",
      "command": "PLEXUS_URL=http://localhost bun run prep-dev --save"
    },
    "prep-dev:live": {
      "type": "task",
      "command": "PLEXUS_URL=http://localhost bun run prep-dev --live"
    },
    "prep-dev:clear": {
      "type": "task",
      "command": "PLEXUS_URL=http://localhost bun run prep-dev --clear"
    },
    "prep-dev:reset": {
      "type": "task",
      "command": "PLEXUS_URL=http://localhost bun run prep-dev --reset"
    },
    "deploy:staging": {
      "type": "command",
      "command": "bun run deploy:staging"
    },
    "release": {
      "type": "command",
      "command": "bun run release"
    }
  },
  "metadataGeneration": {
    "branchName": {
      "instructions": "Use the format <type>/<scope>-<short-desc>. Keep it lowercase ASCII with hyphens only in the short description. Use a concise scope and description. Examples: feat/auth-session-refresh, fix/websocket-host-check, chore/ci-node-upgrade."
    },
    "commitMessage": {
      "instructions": "Follow Conventional Commits. Describe only the final state of the codebase, not the steps taken to get there. Do not mention implementation process, exploration, refactors that were discarded, or debugging work unless they are part of the shipped result.\n\n## Example Output\n```\nfeat(auth): add password reset functionality\n\n- Add forgot password form\n- Implement email verification flow\n- Add password reset endpoint\n```\n\n"
    },
    "pullRequest": {
      "instructions": "Describe the final state of the change for another engineer. Do not describe the implementation journey, debugging process, or discarded approaches. Do not include a Testing section, checklist, marketing language, hype, or sales framing. Keep it factual, technical, and concise."
    },
    "agentTitle": {
      "instructions": "Choose a concise 2-5 word title. Use plain ASCII only. No emojis, smart quotes, dashes, or other Unicode characters. Prefer short, descriptive noun phrases or imperative phrases"
    }
  }
}
